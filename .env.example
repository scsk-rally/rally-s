# Copy to .env.local only for isolated local development.
# Keep all real credentials out of Git. Never copy the old production .env.local.
# Production requires AUTH_SECRET (32+ characters) and a separate DATABASE_URL.
DATABASE_URL=
AUTH_SECRET=
INITIAL_ADMIN_ID=
INITIAL_ADMIN_PASSWORD=
INITIAL_COMPANIES_JSON=[]
BLOB_READ_WRITE_TOKEN=
